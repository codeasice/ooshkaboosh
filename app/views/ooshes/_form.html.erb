<%= form_for(@oosh) do |f| %>
  <% if @oosh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@oosh.errors.count, "error") %> prohibited this oosh from being saved:</h2>

      <ul>
      <% @oosh.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :full_text %><br />
    <%= f.text_field :full_text %>
  </div>
  <div class="field">
    <%= f.label :keyword %><br />
    <%= f.text_field :keyword %>
  </div>
  <div class="field">
    <%= f.label :extra_text %><br />
    <%= f.text_field :extra_text %>
  </div>
  <div class="field">
    <%= f.label :stamp %><br />
    <%= f.datetime_select :stamp %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
